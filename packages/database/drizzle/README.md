# Drizzle Migrations

This directory contains auto-generated SQL migration files.

**Do not edit these files manually.**

Generate new migrations with:

```bash
pnpm db:generate
```

Apply migrations with:

```bash
pnpm db:migrate
```
