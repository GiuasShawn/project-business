# @loom/types

Shared TypeScript types for Project Loom.

## Purpose

Provides type definitions used across all packages and applications.

## Usage

```typescript
import { UUID, ApiResponse, UserRole } from '@loom/types'
```

## Rules

- No runtime code
- Types only
- No dependencies on frameworks
